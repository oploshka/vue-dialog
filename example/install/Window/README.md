# Window

Application-level window presets for the core modal controller.

```text
Window/
├─ Modal/
├─ Dialog/
├─ Fullscreen/
├─ SidebarLeft/
└─ SidebarRight/
```

Each preset starts as configuration/facade only. Presenter/Position/Wrapper/etc. are added inside the preset only when its behavior differs from the core defaults.

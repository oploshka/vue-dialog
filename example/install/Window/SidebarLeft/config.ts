export const sidebarLeftConfig = {
  // presenterComp: null,
  // presenterProps: null,
  // wrapComp: null,
  // wrapProps: null,
  // bridgeComp: null,
  // bridgeProps: null,
  // closeOnEsc: true,
  // closeOnBackdrop: true,
}
